export default interface Deliveryman {
    // _id: string;
    first_name: string;
    last_name:  string;
	email: string;
	password: string;
	phone: string;
	not_locked: boolean;
	join_date: string;
	status: string;
}